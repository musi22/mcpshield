FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx pydantic

COPY apps/mock_server /app/apps/mock_server

EXPOSE 8001

CMD ["uvicorn", "apps.mock_server.server:app", "--host", "0.0.0.0", "--port", "8001"]
